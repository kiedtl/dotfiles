;;
;; kiedtl's polybar configuration
;; https://github.com/kiedtl/dotfiles
;;
;; (c) Kiëd Llaentenn
;; check the LICENSE for more info.
;;


;;       ---
;;  the actual bar
;;       ---

[settings]
pseudo-transparency = true

[bar/bottom]
override-redirect = false
wm-restack        =
fixed-center      = true

monitor  =
bottom   = true
width    = 100%
height   = 20
offset-y = 0
offset-x = 0

background    = ${colors.bg}
foreground    = ${colors.fg}
font-0        = ${fonts.cursed}
font-1        = ${fonts.cursed}
font-2        = ${fonts.cursed}
separator     = " "
padding-left  = 0
padding-right = 0


modules-left   = rcmen battery-label battery
modules-center = memory-label memory filesystem-label filesystem network-label network
modules-right  = volume-label volume brightness-label brightness kbd-label kbd

radius = 0
border-bottom-size = 0
border-color  = ${colors.fg}

[bar/top]
override-redirect = false
wm-restack        =
fixed-center      = true

monitor  =
bottom   = false
width    = 100%
height   = 20
offset-y = 0
offset-x = 0

background    = ${colors.bg}
foreground    = ${colors.fg}
font-0        = ${fonts.cursed}
separator     = " "
padding-left  = 0
padding-right = 0


modules-left   = i3
modules-center = title
modules-right  = date

radius = 0
border-bottom-size = 0
border-color  = ${colors.fg}

;;        ----
;;  colors and fonts
;;        ----

[colors]
fg = ${xrdb:polybar.fg:#e1e2e2}
bg = ${xrdb:polybar.bg:#192222}
ac = ${xrdb:color2:#884444}

tier0 = ${xrdb:color8:#cacbca}
tier1 = ${xrdb:color3:#cacbca}

[fonts]
fixed        = "fixed:size=16:style=bold;2"
fixed-bold   = "fixed:size=16:style=bold;2"
chicago      = "Chicago:size=16:style=bold;2"
gomme        = "gomme:size=16;2"
ttyp0        = "ttyp0:size=10;2"
ttyp0-bold   = "ttyp0:size=10:style=Bold;2"
bitocra      = "bitocra13;2"
bitocra-bold = "bitocra13:style=Bold;2"
hack         = "hack:size=11;2"
hack-bold    = "hack:size=11:style=Bold;2"
tamzen       = "tamzen:size=16;2"
tamzen-bold  = "tamzen:size=16:style=Bold;2"
siji         = "siji;2"
cursed       = "cursed:size=20;3"
spleen       = "spleen:size=16;3"

;;     ---
;;  formatting
;;     ---

[module/tier-0l]
type = custom/text
content = ""
content-foreground = ${colors.tier1}
content-background = ${colors.tier0}

[module/tier-1l]
type = custom/text
content = ""
content-foreground = ${colors.tier0}

[module/tier-0r]
type = custom/text
content = ""
content-foreground = ${colors.tier0}

[module/tier-1r]
type = custom/text
content = ""
content-foreground = ${colors.tier1}
content-background = ${colors.tier0}

[module/tire-2r]
type = custom/text
content = "%{F-}"

;;    ---
;;  modules
;;    ---

[module/i3]
type = internal/i3

; Only show workspaces defined on the same monitor as the bar
pin-workspaces = true

; This will split the workspace name on ':'
strip-wsnumbers = false

enable-click = true
enable-scroll = true
wrapping-scroll = true

;ws-icon-0 = 1;
;ws-icon-1 = 2;
;ws-icon-2 = 3;
;ws-icon-3 = 4;
;ws-icon-4 = 5;

ws-icon-0 = 1;I
ws-icon-1 = 2;II
ws-icon-2 = 3;III
ws-icon-3 = 4;IV
ws-icon-4 = 5;V

label-focused = %icon%
label-focused-padding = 1
label-focused-foreground = ${colors.bg}
label-focused-background = ${colors.ac}

label-unfocused = %icon%
label-unfocused-padding = 1

[module/rcmen]
type = custom/text
click-left = rcmen
;content = "  "
content = "  @  "
content-background = ${colors.ac}
content-foreground = ${colors.bg}

[module/apple-logo]
type = custom/text
click-left = ndmen
content = "    "

[module/menu-window]
type = custom/menu
label-open = "Window  "
label-close = " x "
label-separator = " "
menu-0-0 = New
menu-0-0-exec = winm n
menu-0-1 = Close
menu-0-1-exec = winm c
menu-0-2 = Kill
menu-0-2-exec = winm k
menu-0-3 = Move
menu-0-3-exec = winm m
menu-0-4 = "Resize "
menu-0-4-exec = winm r

[module/menu-special]
type = custom/menu
label-open = Special
label-close = " x "
label-separator = " "
menu-0-0 = Restart
menu-0-0-exec = restart
menu-0-1 = Shutdown
menu-0-1-exec = halt

[module/title]
type = internal/xwindow
;format =  <label>
format = <label>
format-padding = 0

label = %title%
label-maxlen = 35

[module/workspaces]
type = internal/xworkspaces
enable-click = true
enable-scroll = true

format = <label-state>
format-padding = 2
;; pin-workspaces = true

label-active = "%name%"
label-occupied =
label-urgent =
label-empty =

[module/volume-label]
type = custom/text
content = "vol:"
content-foreground = ${colors.ac}

[module/volume]
type = internal/pulseaudio

; Available tags: <label-volume> (default) <ramp-volume> <bar-volume>
format-volume = <bar-volume>
format-volume-padding = 1

; Available tags: <label-muted> (default) <ramp-volume> <bar-volume>
format-muted = <label-muted>
format-muted-padding = 1

; Available tokens:  %percentage% (default)
label-volume = %percentage%%

; Available tokens: %percentage% (default)
format-muted-prefix =
label-muted = "Muted"
label-muted-foreground = ${colors.fg}

; Only applies if <ramp-volume> is used
ramp-volume-0 = #
ramp-volume-1 = #
ramp-volume-2 = #
ramp-volume-3 = #
ramp-volume-4 = #

; Only applies if <bar-volume> is used
bar-volume-width = 10
bar-volume-gradient = false

bar-volume-indicator = 
bar-volume-indicator-foreground = ${colors.bi}
bar-volume-indicator-font = 2

bar-volume-fill = #
bar-volume-foreground-0 = ${colors.ac}
bar-volume-foreground-1 = ${colors.ac}
bar-volume-foreground-2 = ${colors.ac}
bar-volume-foreground-3 = ${colors.ac}
bar-volume-foreground-4 = ${colors.ac}
bar-volume-foreground-5 = ${colors.ag}
bar-volume-foreground-6 = ${colors.ag}
bar-volume-foreground-7 = ${colors.ag}
bar-volume-foreground-8 = ${colors.ag}
bar-volume-fill-font = 2

bar-volume-empty = .
bar-volume-empty-foreground = ${colors.fg}
bar-volume-empty-font = 2

; If defined, it will replace <ramp-volume> when headphones are plugged in to
; `headphone_control_numid`. If undefined, <ramp-volume> will be used for both.
; Only applies if <ramp-volume> is used
ramp-headphones-0 = #
ramp-headphones-1 = #

[module/brightness-label]
type = custom/text
content = "bri:"
content-foreground = ${colors.ac}

[module/brightness]
type = internal/xbacklight

; Use the following command to list available cards:
; $ ls -1 /sys/class/backlight/
card = intel_backlight

; Available tags: <label> (default) <ramp> <bar>
format = <label>
format-padding = 1

; Available tokens: %percentage% (default)
label = %percentage%%

bar-width = 10
bar-gradient = false

bar-indicator = 
bar-indicator-foreground = ${color.bi}
bar-indicator-font = 2

bar-fill = #
bar-foreground-0 = ${color.bn}
bar-foreground-1 = ${color.bn}
bar-foreground-2 = ${color.bn}
bar-foreground-3 = ${color.bm}
bar-foreground-4 = ${color.bm}
bar-foreground-5 = ${color.bd}
bar-foreground-6 = ${color.bd}
bar-foreground-7 = ${color.bd}
bar-foreground-8 = ${color.bd}
bar-fill-font = 2

bar-empty = #
bar-empty-foreground = ${color.be}
bar-empty-font = 2

[module/battery-label]
type = custom/text
;content = "⌨"
content = "bat:"
content-foreground = ${colors.ac}

[module/battery]
type = internal/battery

; This is useful in case the battery never reports 100% charge
; full-at = 99

; Use the following command to list batteries and adapters:
; $ ls -1 /sys/class/power_supply/
battery = BAT0
adapter = ADP0

; If an inotify event haven't been reported in this many seconds,
; manually poll for new values. Needed as a fallback for systems that don't
; report events on sysfs/procfs. (0 = disabled)
poll-interval = 2

; see "man date" for details on how to format the time string
; NOTE: if you want to use syntax tags here you need to use %%{...}
time-format = %H:%M

; Available tags:
;   <label-charging> (default)
;   <bar-capacity>
;   <ramp-capacity>
format-charging = +<bar-capacity>
;format-charging-prefix = "# "

; Available tags:
;   <label-discharging> (default)
;   <bar-capacity>
;   <ramp-capacity>
;   <animation-discharging>
format-discharging = :<bar-capacity>
;format-discharging-prefix = "# "

; Available tags:
;   <label-full> (default)
;   <bar-capacity>
;   <ramp-capacity>
;format-full = <ramp-capacity> <label-full>

; Available tokens:
;   %percentage% (default)
;   %time%
;   %consumption% (shows current charge rate in watts)
label-charging = "%percentage%% (%time%)"

; Available tokens:
;   %percentage% (default)
;   %time%
;   %consumption% (shows current discharge rate in watts)
label-discharging = "%percentage%% (%time%)"

; Available tokens:
;   %percentage% (default)
label-full = 100% charged

; Only applies if <bar-capacity> is used
bar-capacity-width = 10
bar-capacity-gradient = false

bar-capacity-indicator = 
bar-capacity-indicator-foreground = ${color.bi}
bar-capacity-indicator-font = 2

bar-capacity-fill = #
bar-capacity-foreground-0 = ${colors.fc}
bar-capacity-foreground-1 = ${colors.fc}
bar-capacity-foreground-2 = ${colors.fc}
bar-capacity-foreground-3 = ${colors.fc}
bar-capacity-foreground-4 = ${colors.fc}
bar-capacity-foreground-5 = ${colors.fc}
bar-capacity-foreground-6 = ${colors.fc}
bar-capacity-foreground-7 = ${colors.fc}
bar-capacity-foreground-8 = ${colors.fc}
bar-capacity-fill-font = 2

bar-capacity-empty = .
bar-capacity-empty-foreground = ${colors.fg}
bar-capacity-empty-font = 2

; Only applies if <ramp-capacity> is used
ramp-capacity-0 = #
ramp-capacity-1 = #
ramp-capacity-2 = #
ramp-capacity-3 = #
ramp-capacity-4 = #
ramp-capacity-5 = #
ramp-capacity-6 = #
ramp-capacity-7 = #
ramp-capacity-8 = #
ramp-capacity-9 = #

[module/kbd-label]
type = custom/text
;content = "⌨"
content = "kbd:"
content-foreground = ${colors.ac}

[module/kbd]
type = internal/xkeyboard
format = <label-layout> <label-indicator>
format-spacing = 1

[module/filesystem-label]
type = custom/text
;content = ""
content = "fs:"
content-foreground = ${colors.ac}

[module/filesystem]
type = internal/fs

mount-0 = /
interval = 10 
fixed-values = true

;format-mounted = "<ramp-capacity> <label-mounted>  "
format-mounted = "<label-mounted>  "
format-unmounted = "<label-unmounted>  "

label-mounted = %free%
label-unmounted = %mountpoint%: unmounted!

bar-used-indicator =
bar-used-width = 10
bar-used-fill = |
bar-used-empty = |
bar-used-foreground-0 = ${colors.fg}
bar-used-foreground-1 = ${colors.fg}
bar-used-foreground-2 = ${colors.fg}
bar-used-foreground-3 = ${colors.fg}
bar-used-empty-foreground = ${colors.fg}

ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 

ramp-capacity-foreground = ${colors.fg}

[module/date-label]
type = custom/text
content = "  date  "

[module/date]
type = internal/date
interval = 1.0

time = "  %H%M "
time-alt = "  %Y-%m-%d "

format = "<label>"
format-padding = 1
format-background = ${colors.ac}
format-foreground = ${colors.bg}

label = "%time%"

[module/network-label]
type = custom/text
;content = ""
;content-foreground = ${colors.ac}
content = "net:"
content-foreground = ${colors.ac}

[module/network]
type = internal/network
interface = wlan0

interval  = 5
accumulate-stats = true
unknown-as-up = false

format-padding = 0

format-connected = <label-connected>
format-disconnected = <label-disconnected>
;format-connected = <ramp-signal> <label-connected>
;format-disconnected = <label-disconnected>

label-connected = "%essid% "
;; %downspeed:8% %upspeed:8%"
;label-disconnected = " nah! "
label-disconnected = "nah! "

ramp-signal-0 = 
ramp-signal-1 = 
ramp-signal-2 = 
ramp-signal-3 = 

[module/memory-label]
type = custom/text
;content = ""
;content-foreground = ${colors.ac}
content = "mem:"
content-foreground = ${colors.ac}

[module/memory]
type = internal/memory
interval = 2

;format = "<ramp-used> <label>  "
format = "<label>  "
format-prefix =

label = %mb_used%

bar-used-indicator =
bar-used-width = 10
bar-used-fill = |
bar-used-empty = |
bar-used-foreground-0 = ${colors.ac}
bar-used-foreground-1 = ${colors.ac}
bar-used-foreground-2 = ${colors.ac}
bar-used-foreground-3 = ${colors.ac}
bar-used-empty-foreground = ${colors.ac}

ramp-used-0 = 
ramp-used-1 = 
ramp-used-2 = 
ramp-used-3 = 

ramp-used-foreground = ${colors.fg}
